"""Web Register And Sub2API Import Automation.

A FastAPI service that automates web registration for your OWN / authorized
web projects, optionally performs phone verification via a pluggable SMS
provider (62-US or 5sim), captures the OAuth callback, and imports the
resulting OpenAI OAuth account into a locally deployed Sub2API instance.
"""

__version__ = "1.0.0"
